
```
<?
AddEventHandler("iblock", "OnAfterIBlockElementAdd", Array("MyClass", "OnAfterIBlockElementAddHandler"));

class MyClass
{
   function OnAfterIBlockElementAddHandler(&$arFields)
   {
   $arSelect = Array("CATALOG_PRICE_7");
   $arFilter = Array("IBLOCK_ID"=>119, "ID"=>$arFields['ID']);

   $arProducts = CIBlockElement::GetList(Array(), $arFilter, false, false, $arSelect);
   $ob = $arProducts->GetNextElement();
   $fields = $ob->GetFields();

       global $arPrice;
       $arPrice = $fields['CATALOG_PRICE_7'];
   }
}
?>

<?
$el = new CIBlockElement;

$arLoadProductArray = Array(
	//"IBLOCK_SECTION_ID" => 404,          // элемент лежит в корне раздела
  "IBLOCK_ID"      => 2,
  "NAME"           => "Элемент",
  "ACTIVE"         => "N",            // активен
  "PREVIEW_TEXT"   => "текст для списка элементов",
  "DETAIL_TEXT"    => "текст для детального просмотра",
  );

if($PRODUCT_ID = $el->Add($arLoadProductArray))
  echo "New ID: ".$PRODUCT_ID;
else
  echo "Error: ".$el->LAST_ERROR;


$arFields = Array(
    "PRODUCT_ID" => $PRODUCT_ID,
    "CATALOG_GROUP_ID" => 1,
    "PRICE" => 777,
    "CURRENCY" => "RUB",
);

$res = CPrice::GetList(
        array(),
        array(
                "PRODUCT_ID" => $PRODUCT_ID,
                "CATALOG_GROUP_ID" => $PRICE_TYPE_ID
            )
    );

if ($arr = $res->Fetch())
{
    CPrice::Update($arr["ID"], $arFields);
}
else
{
    CPrice::Add($arFields);
}
 var_dump($arPrice);
?>
```