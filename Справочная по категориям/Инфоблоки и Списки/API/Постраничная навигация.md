
```
\Bitrix\Main\Loader::includeModule('iblock');

  

$arSelect = ['ID', 'NAME', 'PROPERTY_BX'];

$arFilter = ['IBLOCK_ID' => 42];

$arNavStartParams = [

          'iNumPage'  => 1,

          'nPageSize' => 2,

];

$arGroupBy = false;

$arSort = ['ID' => 'ASC'];

  

$res = \CIBlockElement::GetList($arSort, $arFilter, $arGroupBy, $arNavStartParams, $arSelect);

while ($ob = $res->GetNext())

{

echo $ob['ID'] . ' - ' . $ob['NAME'] . ': ' . $ob['PROPERTY_BX_VALUE_ID'] . '<br>';

}


$arSelect = Array("ID", "IBLOCK_ID", "NAME");

$arFilter = Array("IBLOCK_ID"=>IntVal(2), "ACTIVE"=>"Y");

  
$res = CIBlockElement::GetList(Array(), $arFilter, false, Array("nPageSize"=>10), $arSelect);

$res->NavStart();


$all_element = $res->NavRecordCount;

$all_page = $res->NavPageCount;

```