
```
\Bitrix\Main\Loader::includeModule('iblock');

	$IBLOCK_ID = 2; 
	$properties = CIBlockProperty::GetList(
		Array("sort"=>"asc", "name"=>"asc"), 
		Array("ACTIVE"=>"Y", "IBLOCK_ID"=>$IBLOCK_ID)
		);
    # Получаем список свойств
	while ($prop_fields = $properties->GetNext())
	{
		if($prop_fields["SORT"] == 110) 
		{ 
			# Обновляем фичи
			\Bitrix\Iblock\Model\PropertyFeature::setFeatures($prop_fields["ID"], [      
    			["FEATURE_ID"=>"DETAIL_PAGE_SHOW", "IS_ENABLED" => "Y", "MODULE_ID" => "iblock"], 
    			["FEATURE_ID"=>"LIST_PAGE_SHOW", "IS_ENABLED" => "Y", "MODULE_ID" => "iblock"]  
			]);
            # Обновляем параметры свойства
			$arFields = Array('SMART_FILTER' => 'Y', 'DISPLAY_EXPANDED' => 'Y', 'IBLOCK_ID' => $IBLOCK_ID);

			$ibp = new CIBlockProperty;
    		$ibp->Update($prop_fields['ID'], $arFields);
		}
	}
```